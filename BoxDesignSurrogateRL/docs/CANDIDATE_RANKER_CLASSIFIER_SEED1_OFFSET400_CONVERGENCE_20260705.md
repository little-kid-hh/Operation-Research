# Candidate Classifier Ranker Seed1 Offset400 Convergence Comparison, 2026-07-05

## Purpose

Run the fifth seed-specific initial-condition replicate of the shared-cache
convergence-path protocol. Exact staged generates the initial K=10 box set by
k-means with `--seed 1` on test[400,500), and the ranker+audit path reuses
that exact run's `initial_boxes.json`.

The comparison answers:

> Under the seed-1 initial box protocol on the fifth 100-order test window,
> can a time-budgeted learned ranker followed by exact audit match exact staged
> convergence while using fewer exact MILP oracle resources?

## Setup

- Dataset: OR2023 test split orders with zero-based indices `[400, 500)`.
- K: 10.
- Initial boxes: seed-1 k-means generated by exact staged baseline.
- Schedule: `0.25:1000`.
- Oracle: Java/Gurobi MILP, `label_6ori`.
- Coverage repair: `geometric_expand` for both exact and ranker+audit.
- Exact convergence wall-clock budget: none.
- Ranker wall-clock budget: `--ranker-max-elapsed-seconds 180`.
- Audit wall-clock budget: none.
- Ranker budget sequence: `10,20,30,40,50`.
- Code version: `5036d3f`.

Exact and ranker+audit were run through:

```text
BoxDesignSurrogateRL/scripts/run_ranker_window_protocol.py
```

Protocol output:

```text
BoxDesignSurrogateRL/results/ranker_window_protocol_seed1_offset400_20260705/protocol_20260705_042122
```

Auto-generated summary:

```text
BoxDesignSurrogateRL/docs/SEED1_OFFSET400_RANKER_AUTO_SUMMARY_20260705.md
```

## Main Comparison

| metric | exact staged convergence | RF top50 180s + exact audit, shared cache |
| --- | ---: | ---: |
| final PF | 1.9910783173 | 1.9910783173 |
| coverage | 1.000 | 1.000 |
| uncovered orders | 0 | 0 |
| MILP validations | 7920 | 6770 |
| uncached boxes | 716 | 660 |
| subprocess seconds | 397.6499 | 363.2582 |
| elapsed seconds | 528.6209 | 483.5014 |

The ranker path plus exact audit reaches the same final PF and full coverage
as exact staged from the same seed-1 initial boxes.

The cost comparison favors the ranker path:

- 14.5% fewer MILP candidate validations.
- 7.8% fewer uncached boxes.
- 8.6% lower Java/Gurobi subprocess time.
- 8.5% lower wall-clock time.

The ranker-only 180-second phase stopped at PF `2.0714220263` with full
coverage. The exact audit then continued from those boxes to PF
`1.9910783173`.

## Interpretation

This fifth seed-1 window completes a seed-specific five-window pass over the
500-order test split. Ranker+audit preserves or improves final PF and full
coverage in every seed-1 window while reducing all primary cost metrics in
aggregate.

Across seed1:test[0,500) evaluated as five 100-order windows, ranker+audit
reduces validations by 14.3%, uncached boxes by 6.4%, Java/Gurobi subprocess
time by 6.7%, and wall-clock time by 7.2%.
