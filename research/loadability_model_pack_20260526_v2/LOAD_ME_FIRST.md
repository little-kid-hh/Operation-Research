# Load Me First

1. Rebuild the same feature columns.
2. Import `load_model_examples.py`.
3. Call the relevant loader.
4. Run `predict_proba` or `predict`.

The saved models will not work on a table with different columns.
