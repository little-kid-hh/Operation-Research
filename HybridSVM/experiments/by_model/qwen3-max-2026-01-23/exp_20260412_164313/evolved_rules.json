[
  {
    "id": "seed_0",
    "code": "# -*- coding: utf-8 -*-\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Seed rule 0: never override (trivial baseline)\n    return -1\n",
    "fn_corr": 0,
    "fp_corr": 0,
    "fn_miss": 65,
    "fp_over": 116,
    "score": 0.0,
    "fitness_history": [
      0.0
    ],
    "island_id": 0,
    "easy_overrides": 0
  },
  {
    "id": "seed_1",
    "code": "# -*- coding: utf-8 -*-\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts   = features.get('sku_counts', 0)\n    fill_ratio   = features.get('fill_ratio', 1.0)\n    max_asr      = features.get('max_asr', 1.0)\n    sku_avg_vol  = features.get('sku_average_volume', 0)\n    vehicle_cap  = features.get('vehicle_capacity', 45000)\n    total_vol    = sku_counts * sku_avg_vol\n\n    # Correct FP: very high fill_ratio with many small items — likely infeasible\n    if fill_ratio > 0.93 and sku_counts > 15:\n        return 0\n\n    # Correct FN: low fill_ratio but SVM is uncertain\n    if fill_ratio < 0.70 and svm_prob < 0.60:\n        return 1\n\n    return -1\n",
    "fn_corr": 0,
    "fp_corr": 6,
    "fn_miss": 65,
    "fp_over": 110,
    "score": 0.025760334247106893,
    "fitness_history": [
      0.025861818744985016
    ],
    "island_id": 0,
    "easy_overrides": 91
  },
  {
    "id": "iter1_cand0_c85cc8",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_cap = features.get('spare_capacity', 0)\n    wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_avg_vol = features.get('sku_average_volume', 0)\n    vehicle_cap = 45000  # standard vehicle capacity\n    \n    # Correct FN cases: low fill_ratio but SVM under-confident\n    # From FN analysis: typical fill_ratio around 5000-7000 (which is ~0.11-0.16 of 45000)\n    # But note: fill_ratio in data appears to be absolute volume, not fraction!\n    # Checking feature scales: wl_to_vehicle_wl_total max is 2111.25, but fill_ratio examples are 5282, 6228, 7126\n    # This suggests fill_ratio might actually be total volume in cubic units, not fraction\n    # Given vehicle_capacity = 45000, then actual fill fraction = fill_ratio / 45000\n    actual_fill_frac = fill_ratio / vehicle_cap if vehicle_cap > 0 else 0\n    \n    # FN correction: when actual fill fraction is low (< 0.2) but SVM is uncertain (prob < 0.6)\n    # This matches FN examples: fill_ratio ~5000-7000 → ~0.11-0.16 fraction\n    if actual_fill_frac < 0.2 and svm_prob < 0.6:\n        return 1\n    \n    # FP correction: high fill fraction with moderate item count but high aspect ratio items\n    # FP examples show fill_ratio 18000-21000 (~0.4-0.47 fraction) with sku_counts 7-8\n    # High aspect ratio items (>6.0) are harder to pack efficiently\n    if actual_fill_frac > 0.4 and actual_fill_frac < 0.5 and sku_counts <= 10 and max_asr > 6.0:\n        return 0\n    \n    # Additional FP correction: very high total wall area usage with low minimum wall area\n    # This indicates items have varying footprints that may not pack well\n    # From FP analysis: wl_to_vehicle_wl_min is quite low (mean ~47) while total is high (~1100)\n    # Vehicle wall area capacity seems to be around 1500 based on feature scales\n    if wl_total > 1000 and wl_min < 40 and sku_counts <= 10:\n        return 0\n    \n    return -1",
    "fn_corr": 65,
    "fp_corr": 2,
    "fn_miss": 0,
    "fp_over": 114,
    "score": 0.4990136428846937,
    "fitness_history": [
      0.5086201818959233
    ],
    "island_id": 0,
    "easy_overrides": 438
  },
  {
    "id": "iter1_cand0_c85cc8_reset_2589",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_cap = features.get('spare_capacity', 0)\n    wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_avg_vol = features.get('sku_average_volume', 0)\n    vehicle_cap = 45000  # standard vehicle capacity\n    \n    # Correct FN cases: low fill_ratio but SVM under-confident\n    # From FN analysis: typical fill_ratio around 5000-7000 (which is ~0.11-0.16 of 45000)\n    # But note: fill_ratio in data appears to be absolute volume, not fraction!\n    # Checking feature scales: wl_to_vehicle_wl_total max is 2111.25, but fill_ratio examples are 5282, 6228, 7126\n    # This suggests fill_ratio might actually be total volume in cubic units, not fraction\n    # Given vehicle_capacity = 45000, then actual fill fraction = fill_ratio / 45000\n    actual_fill_frac = fill_ratio / vehicle_cap if vehicle_cap > 0 else 0\n    \n    # FN correction: when actual fill fraction is low (< 0.2) but SVM is uncertain (prob < 0.6)\n    # This matches FN examples: fill_ratio ~5000-7000 → ~0.11-0.16 fraction\n    if actual_fill_frac < 0.2 and svm_prob < 0.6:\n        return 1\n    \n    # FP correction: high fill fraction with moderate item count but high aspect ratio items\n    # FP examples show fill_ratio 18000-21000 (~0.4-0.47 fraction) with sku_counts 7-8\n    # High aspect ratio items (>6.0) are harder to pack efficiently\n    if actual_fill_frac > 0.4 and actual_fill_frac < 0.5 and sku_counts <= 10 and max_asr > 6.0:\n        return 0\n    \n    # Additional FP correction: very high total wall area usage with low minimum wall area\n    # This indicates items have varying footprints that may not pack well\n    # From FP analysis: wl_to_vehicle_wl_min is quite low (mean ~47) while total is high (~1100)\n    # Vehicle wall area capacity seems to be around 1500 based on feature scales\n    if wl_total > 1000 and wl_min < 40 and sku_counts <= 10:\n        return 0\n    \n    return -1",
    "fn_corr": 65,
    "fp_corr": 2,
    "fn_miss": 0,
    "fp_over": 114,
    "score": 0.4990136428846937,
    "fitness_history": [
      0.5086201818959233
    ],
    "island_id": 1,
    "easy_overrides": 438
  },
  {
    "id": "iter3_cand0_b8332b",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    spare_cap = features.get('spare_capacity', 0)\n    vehicle_cap = 45000  # standard vehicle capacity\n    \n    # Calculate actual fill fraction\n    actual_fill_frac = fill_ratio / vehicle_cap if vehicle_cap > 0 else 0\n    \n    # FN Correction: Very low fill ratio cases where SVM is under-confident\n    # FN examples show fill_ratio 5282-7126 (~0.12-0.16 fraction) with sku_counts around 11\n    # SVM prob typically < 0.5 for these cases\n    if actual_fill_frac <= 0.18 and svm_prob < 0.6 and sku_counts >= 8 and sku_counts <= 14:\n        return 1\n    \n    # FP Correction: Moderate fill ratio with high aspect ratio items that are hard to pack\n    # FP examples show fill_ratio 18000-21000 (~0.4-0.47 fraction), sku_counts 7-8, max_asr often high\n    # The linear SVM overestimates feasibility for high aspect ratio items\n    if (actual_fill_frac >= 0.38 and actual_fill_frac <= 0.52 and \n        sku_counts <= 10 and max_asr >= 5.7):\n        return 0\n    \n    # FP Correction: High total wall area with very low minimum wall area indicates poor packing geometry\n    # This creates gaps that make packing infeasible despite moderate volume usage\n    # FP cases show wl_total > 1000 and wl_min < 45\n    if wl_total > 1000 and wl_min < 42 and sku_counts <= 12 and actual_fill_frac >= 0.35:\n        return 0\n    \n    # Additional FN correction: Cases with very high spare capacity but SVM still says NO\n    # These are clearly feasible due to abundant space\n    if spare_cap >= 25000 and svm_prob < 0.5:\n        return 1\n        \n    # Additional FP correction: High variance in length combined with moderate fill ratio\n    # Indicates irregular shapes that are hard to pack efficiently\n    sku_length_var = features.get('sku_length_var', 0)\n    if (actual_fill_frac >= 0.4 and actual_fill_frac <= 0.55 and \n        sku_length_var >= 50 and sku_counts <= 10 and svm_prob > 0.6):\n        return 0\n    \n    return -1",
    "fn_corr": 59,
    "fp_corr": 0,
    "fn_miss": 6,
    "fp_over": 116,
    "score": 0.44813123529076593,
    "fitness_history": [
      0.4538458968640432
    ],
    "island_id": 2,
    "easy_overrides": 292
  },
  {
    "id": "iter8_cand0_04977a",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    spare_cap = features.get('spare_capacity', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    vehicle_cap = 45000  # standard vehicle capacity\n    \n    # Calculate actual fill fraction\n    actual_fill_frac = fill_ratio / vehicle_cap if vehicle_cap > 0 else 0\n    \n    # FN Correction: Very low fill ratio cases where SVM is under-confident\n    # Based on FN examples: fill_ratio 5282-7126 (~0.12-0.16), sku_counts around 11, low SVM prob\n    if actual_fill_frac <= 0.18 and svm_prob < 0.55 and 8 <= sku_counts <= 14:\n        return 1\n    \n    # FP Correction: Moderate fill ratio with high aspect ratio items that are hard to pack\n    # FP examples show fill_ratio 18000-21000 (~0.4-0.47), sku_counts 7-8, max_asr often >= 5.8\n    if (0.38 <= actual_fill_frac <= 0.52 and \n        sku_counts <= 10 and max_asr >= 5.8):\n        return 0\n    \n    # FP Correction: High total wall area with very low minimum wall area indicates poor packing geometry\n    # FP cases show wl_total > 1050 and wl_min < 45, creating gaps despite moderate volume usage\n    if wl_total > 1050 and wl_min < 45 and sku_counts <= 12 and actual_fill_frac >= 0.35:\n        return 0\n        \n    # Additional FN correction: Cases with very high spare capacity but SVM still says NO\n    # These are clearly feasible due to abundant space (spare_cap >= 25000 is well above median 15747.5)\n    if spare_cap >= 25000 and svm_prob < 0.5:\n        return 1\n        \n    # Additional FP correction: High variance in length combined with moderate fill ratio\n    # Indicates irregular shapes that are hard to pack efficiently (sku_length_var >= 50 is near p75=53.66)\n    if (0.4 <= actual_fill_frac <= 0.55 and \n        sku_length_var >= 50 and sku_counts <= 10 and svm_prob > 0.6):\n        return 0\n    \n    return -1",
    "fn_corr": 59,
    "fp_corr": 0,
    "fn_miss": 6,
    "fp_over": 116,
    "score": 0.44865964577870593,
    "fitness_history": [
      0.4538458968640432
    ],
    "island_id": 2,
    "easy_overrides": 265
  },
  {
    "id": "iter6_cand0_e1c5ec",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_cap = features.get('spare_capacity', 0)\n    wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    vehicle_capacity = 45000  # standard vehicle capacity\n    \n    # Calculate actual fill fraction\n    actual_fill_frac = fill_ratio / vehicle_capacity if vehicle_capacity > 0 else 0\n    \n    # FN Correction: Very low fill ratio cases where SVM is under-confident\n    # Based on FN examples: fill_ratio 5282-7126 (~0.12-0.16), sku_counts around 11, low SVM prob\n    if actual_fill_frac <= 0.18 and svm_prob < 0.6 and 8 <= sku_counts <= 14:\n        return 1\n    \n    # FP Correction: Moderate fill ratio with high aspect ratio items that are hard to pack\n    # FP examples show fill_ratio 18000-21000 (~0.4-0.47), sku_counts 7-8, max_asr often >= 5.7\n    if (0.38 <= actual_fill_frac <= 0.52 and \n        sku_counts <= 10 and max_asr >= 5.7):\n        return 0\n    \n    # FP Correction: High total wall area with very low minimum wall area indicates poor packing geometry\n    # FP cases show wl_total > 1000 and wl_min < 45, creating gaps despite moderate volume usage\n    if wl_total > 1000 and wl_min < 42 and sku_counts <= 12 and actual_fill_frac >= 0.35:\n        return 0\n        \n    # Additional FN correction: Cases with very high spare capacity but SVM still says NO\n    # These are clearly feasible due to abundant space (spare_cap >= 25000 is well above median 15747.5)\n    if spare_cap >= 25000 and svm_prob < 0.5:\n        return 1\n        \n    # Additional FP correction: High variance in length combined with moderate fill ratio\n    # Indicates irregular shapes that are hard to pack efficiently (sku_length_var >= 50 is above p75=53.66)\n    if (0.4 <= actual_fill_frac <= 0.55 and \n        sku_length_var >= 50 and sku_counts <= 10 and svm_prob > 0.6):\n        return 0\n    \n    return -1",
    "fn_corr": 59,
    "fp_corr": 0,
    "fn_miss": 6,
    "fp_over": 116,
    "score": 0.44813123529076593,
    "fitness_history": [
      0.4538458968640432
    ],
    "island_id": 3,
    "easy_overrides": 292
  },
  {
    "id": "iter10_cand0_c8e9c9",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_cap = features.get('spare_capacity', 0)\n    wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    vehicle_cap = 45000  # standard vehicle capacity\n    \n    actual_fill_frac = fill_ratio / vehicle_cap if vehicle_cap > 0 else 0\n    \n    # FN Correction: Very low fill ratio cases where SVM is under-confident\n    # Based on FN examples: fill_ratio 5282-7126 (~0.12-0.16), sku_counts around 11, low SVM prob\n    if actual_fill_frac <= 0.18 and svm_prob < 0.55 and 8 <= sku_counts <= 14:\n        return 1\n    \n    # FP Correction: Moderate fill ratio with high aspect ratio items that are hard to pack\n    # FP examples show fill_ratio 18000-21000 (~0.4-0.47), sku_counts 7-8, max_asr often >= 5.8\n    if (0.38 <= actual_fill_frac <= 0.52 and \n        sku_counts <= 10 and max_asr >= 5.8):\n        return 0\n    \n    # FP Correction: High total wall area with very low minimum wall area indicates poor packing geometry\n    # FP cases show wl_total > 1050 and wl_min < 45, creating gaps despite moderate volume usage\n    if wl_total > 1050 and wl_min < 45 and sku_counts <= 12 and actual_fill_frac >= 0.35:\n        return 0\n        \n    # Additional FN correction: Cases with very high spare capacity but SVM still says NO\n    # These are clearly feasible due to abundant space (spare_cap >= 25000 is well above median 15747.5)\n    if spare_cap >= 25000 and svm_prob < 0.5:\n        return 1\n        \n    # Additional FP correction: High variance in length combined with moderate fill ratio\n    # Indicates irregular shapes that are hard to pack efficiently (sku_length_var >= 50 is near p75=53.66)\n    if (0.4 <= actual_fill_frac <= 0.55 and \n        sku_length_var >= 50 and sku_counts <= 10 and svm_prob > 0.6):\n        return 0\n    \n    return -1",
    "fn_corr": 59,
    "fp_corr": 0,
    "fn_miss": 6,
    "fp_over": 116,
    "score": 0.44865964577870593,
    "fitness_history": [
      0.4538458968640432
    ],
    "island_id": 3,
    "easy_overrides": 265
  }
]