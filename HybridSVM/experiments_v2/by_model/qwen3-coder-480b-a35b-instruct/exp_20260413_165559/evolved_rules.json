[
  {
    "id": "iter3_cand0_96823a",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    \n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n        # Pattern: Moderate fill with good spare capacity and low dimensional variance\n        if (0.68 <= fill_ratio <= 0.82 and \n            spare_capacity > 10000 and\n            sku_length_var < 45 and \n            sku_width_var < 9 and \n            sku_height_var < 11):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity\n        if 0.55 <= fill_ratio < 0.72 and spare_capacity > 5000:\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        if (sku_average_volume < 2900 and \n            sku_counts <= 13 and \n            spare_capacity > 7000):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        # Pattern: Very high fill ratio with high item count\n        if fill_ratio > 0.91 and sku_counts > 11:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 8:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        if (sku_length_avg > 25 and sku_width_avg > 10.5 and \n            fill_ratio > 0.78 and sku_counts >= 9):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        if wl_to_vehicle_wl_min > 60 and sku_length_var > 50:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        if wl_to_vehicle_wl_max > 190 and fill_ratio > 0.85:\n            return 0\n\n    return -1",
    "fn_corr": 1,
    "fp_corr": 41,
    "fn_miss": 64,
    "fp_over": 75,
    "score": 0.18203022673268435,
    "fitness_history": [
      0.18441594398999345
    ],
    "island_id": 0,
    "easy_overrides": 300
  },
  {
    "id": "seed_0",
    "code": "# -*- coding: utf-8 -*-\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Seed rule 0: never override (trivial baseline)\n    return -1\n",
    "fn_corr": 0,
    "fp_corr": 0,
    "fn_miss": 65,
    "fp_over": 116,
    "score": 0.0,
    "fitness_history": [
      0.0
    ],
    "island_id": 0,
    "easy_overrides": 0
  },
  {
    "id": "iter2_cand0_ad73d9_reset_5648",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    \n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n        # Pattern from analysis: FN cases often have positive spare capacity,\n        # moderate fill ratios, and are not extremely variant in dimensions.\n        # They're \"underestimated\" by the linear model.\n        \n        # Case 1: Moderate fill ratio with sufficient spare capacity and low variance\n        if (0.65 <= fill_ratio <= 0.85 and \n            spare_capacity > 8000 and\n            sku_length_var < 50 and \n            sku_width_var < 10 and \n            sku_height_var < 12):\n            return 1\n            \n        # Case 2: Lower fill ratio with positive spare capacity (conservative SVM)\n        if fill_ratio < 0.7 and spare_capacity > 2000:\n            return 1\n            \n        # Case 3: Items are small on average but not too many\n        if (sku_average_volume < 3000 and \n            sku_counts <= 12 and \n            spare_capacity > 5000):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        # Pattern from analysis: FP cases often have high fill ratios,\n        # high item count, or extreme variations that make packing difficult.\n        \n        # Case 1: Very high fill ratio with many items\n        if fill_ratio > 0.92 and sku_counts > 10:\n            return 0\n            \n        # Case 2: High total area usage with significant item count\n        if wl_to_vehicle_wl_total > 1150 and sku_counts > 9:\n            return 0\n            \n        # Case 3: Large items (high avg dimensions) with moderate-high fill\n        if (sku_length_avg > 24 and sku_width_avg > 10 and \n            fill_ratio > 0.8 and sku_counts >= 8):\n            return 0\n            \n        # Case 4: High minimum WL ratio suggesting large indivisible items\n        if wl_to_vehicle_wl_min > 55 and sku_length_var > 45:\n            return 0\n\n    return -1",
    "fn_corr": 65,
    "fp_corr": 51,
    "fn_miss": 0,
    "fp_over": 65,
    "score": 0.6959570023672063,
    "fitness_history": [
      0.7198270766199328
    ],
    "island_id": 0,
    "easy_overrides": 769
  },
  {
    "id": "iter7_cand0_211a9b",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    sku_std_length = features.get('sku_std_length', 0)\n    sku_std_width = features.get('sku_std_width', 0)\n    sku_std_height = features.get('sku_std_height', 0)\n    l_to_L_ratio_avg = features.get('l_to_L_ratio_avg', 0)\n    w_to_W_ratio_avg = features.get('w_to_W_ratio_avg', 0)\n    h_to_H_ratio_avg = features.get('h_to_H_ratio_avg', 0)\n    l_to_L_ratio_std = features.get('l_to_L_ratio_std', 0)\n    w_to_W_ratio_std = features.get('w_to_W_ratio_std', 0)\n    h_to_H_ratio_std = features.get('h_to_H_ratio_std', 0)\n\n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n\n        # Pattern: Moderate-high fill with good spare capacity and low dimensional variance\n        # This addresses cases where SVM is overly conservative due to high fill but actual packing is possible\n        if (0.7 <= fill_ratio <= 0.85 and \n            spare_capacity > 10000 and\n            sku_length_var < 45 and \n            sku_width_var < 9 and \n            sku_height_var < 11):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity and moderate item count\n        # Addresses cases where low-medium utilization doesn't prevent packing due to ample remaining space\n        if (0.5 <= fill_ratio < 0.7 and \n            spare_capacity > 6000 and\n            sku_counts <= 13):\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        # Handles scenarios where small items can be packed even with moderate counts\n        if (sku_average_volume < 2900 and \n            sku_counts <= 11 and \n            spare_capacity > 7000):\n            return 1\n            \n        # Pattern: Low fill ratio with moderate counts and good spare capacity, even with some variance\n        # Covers cases where despite some size variation, ample space allows for packing\n        if (fill_ratio < 0.6 and \n            sku_counts <= 12 and \n            spare_capacity > 8000 and\n            sku_length_var < 50):\n            return 1\n            \n        # Pattern: Items with relatively uniform dimensions (low std) and moderate fill\n        # These are easier to pack than the SVM might assume\n        if (fill_ratio < 0.75 and\n            sku_std_length < 5.5 and\n            sku_std_width < 2.3 and\n            sku_std_height < 2.8):\n            return 1\n            \n        # New FN correction pattern: High spare capacity with moderate fill and high item count\n        # SVM may underestimate feasibility when there's ample space even with many items\n        if (fill_ratio <= 0.75 and \n            spare_capacity > 12000 and\n            sku_counts >= 10):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        \n        # Pattern: Very high fill ratio with high item count\n        # Classic overpacking scenario\n        if fill_ratio > 0.9 and sku_counts > 9:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        # Indicates potential space constraints despite SVM approval\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 6:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        # Large items become harder to fit as space fills up\n        if (sku_length_avg > 23.5 and sku_width_avg > 9.5 and \n            fill_ratio > 0.75 and sku_counts >= 7):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        # Suggests difficulty in fitting irregularly sized large items\n        if wl_to_vehicle_wl_min > 50 and sku_length_var > 40:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        # Indicates presence of very large items that may not fit in remaining space\n        if wl_to_vehicle_wl_max > 180 and fill_ratio > 0.75:\n            return 0\n            \n        # Pattern: High dimensional variance with moderate fill and item count\n        # Variance in multiple dimensions makes packing complex\n        if (sku_length_var > 45 and \n            sku_width_var > 8 and \n            fill_ratio > 0.65 and \n            sku_counts >= 6):\n            return 0\n            \n        # Pattern: High standard deviations in dimensions with high fill ratio\n        # Dimensional inconsistency leads to packing inefficiencies\n        if (sku_std_length > 6 and \n            sku_std_width > 2.5 and \n            sku_std_height > 3 and \n            fill_ratio > 0.7):\n            return 0\n            \n        # Pattern: High aspect ratio items with moderate-high fill\n        # Long, thin items are notoriously difficult to pack efficiently\n        if max_asr > 6 and fill_ratio > 0.7 and sku_counts > 5:\n            return 0\n            \n        # Pattern: Items that are long relative to vehicle length with moderate variance\n        # Difficult to orient and fit in constrained spaces\n        if (l_to_L_ratio_avg > 0.42 and \n            l_to_L_ratio_std > 0.09 and\n            fill_ratio > 0.65):\n            return 0\n            \n        # New FP correction pattern: Very high fill ratio with moderate item count but low spare capacity\n        # Even moderate counts can be problematic at very high utilization levels\n        if (fill_ratio > 0.92 and \n            sku_counts > 6 and \n            spare_capacity < 5000):\n            return 0\n\n    return -1",
    "fn_corr": 19,
    "fp_corr": 50,
    "fn_miss": 46,
    "fp_over": 66,
    "score": 0.3527808795657108,
    "fitness_history": [
      0.36167058342744624
    ],
    "island_id": 0,
    "easy_overrides": 570
  },
  {
    "id": "iter4_cand0_51b034",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    sku_std_length = features.get('sku_std_length', 0)\n    sku_std_width = features.get('sku_std_width', 0)\n    sku_std_height = features.get('sku_std_height', 0)\n    \n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n        # Pattern: Moderate fill with good spare capacity and low dimensional variance\n        if (0.68 <= fill_ratio <= 0.82 and \n            spare_capacity > 10000 and\n            sku_length_var < 45 and \n            sku_width_var < 9 and \n            sku_height_var < 11):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity\n        if 0.55 <= fill_ratio < 0.72 and spare_capacity > 5000:\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        if (sku_average_volume < 2900 and \n            sku_counts <= 13 and \n            spare_capacity > 7000):\n            return 1\n            \n        # New pattern: Low fill ratio with moderate counts and good spare capacity\n        if (fill_ratio < 0.65 and \n            sku_counts <= 12 and \n            spare_capacity > 8000):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        # Pattern: Very high fill ratio with high item count\n        if fill_ratio > 0.91 and sku_counts > 11:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 8:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        if (sku_length_avg > 25 and sku_width_avg > 10.5 and \n            fill_ratio > 0.78 and sku_counts >= 9):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        if wl_to_vehicle_wl_min > 60 and sku_length_var > 50:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        if wl_to_vehicle_wl_max > 190 and fill_ratio > 0.85:\n            return 0\n            \n        # New pattern: High dimensional variance with moderate fill and item count\n        if (sku_length_var > 55 and \n            sku_width_var > 10 and \n            fill_ratio > 0.75 and \n            sku_counts >= 8):\n            return 0\n            \n        # New pattern: High standard deviations in dimensions with high fill ratio\n        if (sku_std_length > 7 and \n            sku_std_width > 3 and \n            sku_std_height > 3.5 and \n            fill_ratio > 0.8):\n            return 0\n\n    return -1",
    "fn_corr": 36,
    "fp_corr": 41,
    "fn_miss": 29,
    "fp_over": 75,
    "score": 0.44695644712595184,
    "fitness_history": [
      0.45364670907085186
    ],
    "island_id": 1,
    "easy_overrides": 342
  },
  {
    "id": "iter8_cand0_211a9b",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    sku_std_length = features.get('sku_std_length', 0)\n    sku_std_width = features.get('sku_std_width', 0)\n    sku_std_height = features.get('sku_std_height', 0)\n    l_to_L_ratio_avg = features.get('l_to_L_ratio_avg', 0)\n    w_to_W_ratio_avg = features.get('w_to_W_ratio_avg', 0)\n    h_to_H_ratio_avg = features.get('h_to_H_ratio_avg', 0)\n    l_to_L_ratio_std = features.get('l_to_L_ratio_std', 0)\n    w_to_W_ratio_std = features.get('w_to_W_ratio_std', 0)\n    h_to_H_ratio_std = features.get('h_to_H_ratio_std', 0)\n\n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n\n        # Pattern: Moderate-high fill with good spare capacity and low dimensional variance\n        # This addresses cases where SVM is overly conservative due to high fill but actual packing is possible\n        if (0.7 <= fill_ratio <= 0.85 and \n            spare_capacity > 10000 and\n            sku_length_var < 45 and \n            sku_width_var < 9 and \n            sku_height_var < 11):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity and moderate item count\n        # Addresses cases where low-medium utilization doesn't prevent packing due to ample remaining space\n        if (0.5 <= fill_ratio < 0.7 and \n            spare_capacity > 6000 and\n            sku_counts <= 13):\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        # Handles scenarios where small items can be packed even with moderate counts\n        if (sku_average_volume < 2900 and \n            sku_counts <= 11 and \n            spare_capacity > 7000):\n            return 1\n            \n        # Pattern: Low fill ratio with moderate counts and good spare capacity, even with some variance\n        # Covers cases where despite some size variation, ample space allows for packing\n        if (fill_ratio < 0.6 and \n            sku_counts <= 12 and \n            spare_capacity > 8000 and\n            sku_length_var < 50):\n            return 1\n            \n        # Pattern: Items with relatively uniform dimensions (low std) and moderate fill\n        # These are easier to pack than the SVM might assume\n        if (fill_ratio < 0.75 and\n            sku_std_length < 5.5 and\n            sku_std_width < 2.3 and\n            sku_std_height < 2.8):\n            return 1\n            \n        # New FN correction pattern: High spare capacity with moderate fill and high item count\n        # SVM may underestimate feasibility when there's ample space even with many items\n        if (fill_ratio <= 0.75 and \n            spare_capacity > 12000 and\n            sku_counts >= 10):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        \n        # Pattern: Very high fill ratio with high item count\n        # Classic overpacking scenario\n        if fill_ratio > 0.9 and sku_counts > 9:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        # Indicates potential space constraints despite SVM approval\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 6:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        # Large items become harder to fit as space fills up\n        if (sku_length_avg > 23.5 and sku_width_avg > 9.5 and \n            fill_ratio > 0.75 and sku_counts >= 7):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        # Suggests difficulty in fitting irregularly sized large items\n        if wl_to_vehicle_wl_min > 50 and sku_length_var > 40:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        # Indicates presence of very large items that may not fit in remaining space\n        if wl_to_vehicle_wl_max > 180 and fill_ratio > 0.75:\n            return 0\n            \n        # Pattern: High dimensional variance with moderate fill and item count\n        # Variance in multiple dimensions makes packing complex\n        if (sku_length_var > 45 and \n            sku_width_var > 8 and \n            fill_ratio > 0.65 and \n            sku_counts >= 6):\n            return 0\n            \n        # Pattern: High standard deviations in dimensions with high fill ratio\n        # Dimensional inconsistency leads to packing inefficiencies\n        if (sku_std_length > 6 and \n            sku_std_width > 2.5 and \n            sku_std_height > 3 and \n            fill_ratio > 0.7):\n            return 0\n            \n        # Pattern: High aspect ratio items with moderate-high fill\n        # Long, thin items are notoriously difficult to pack efficiently\n        if max_asr > 6 and fill_ratio > 0.7 and sku_counts > 5:\n            return 0\n            \n        # Pattern: Items that are long relative to vehicle length with moderate variance\n        # Difficult to orient and fit in constrained spaces\n        if (l_to_L_ratio_avg > 0.42 and \n            l_to_L_ratio_std > 0.09 and\n            fill_ratio > 0.65):\n            return 0\n            \n        # New FP correction pattern: Very high fill ratio with moderate item count but low spare capacity\n        # Even moderate counts can be problematic at very high utilization levels\n        if (fill_ratio > 0.92 and \n            sku_counts > 6 and \n            spare_capacity < 5000):\n            return 0\n\n    return -1",
    "fn_corr": 19,
    "fp_corr": 50,
    "fn_miss": 46,
    "fp_over": 66,
    "score": 0.3527808795657108,
    "fitness_history": [
      0.36167058342744624
    ],
    "island_id": 1,
    "easy_overrides": 570
  },
  {
    "id": "seed_1",
    "code": "# -*- coding: utf-8 -*-\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    sku_counts   = features.get('sku_counts', 0)\n    fill_ratio   = features.get('fill_ratio', 1.0)\n    max_asr      = features.get('max_asr', 1.0)\n    sku_avg_vol  = features.get('sku_average_volume', 0)\n    vehicle_cap  = features.get('vehicle_capacity', 45000)\n    total_vol    = sku_counts * sku_avg_vol\n\n    # Correct FP: very high fill_ratio with many small items — likely infeasible\n    if fill_ratio > 0.93 and sku_counts > 15:\n        return 0\n\n    # Correct FN: low fill_ratio but SVM is uncertain\n    if fill_ratio < 0.70 and svm_prob < 0.60:\n        return 1\n\n    return -1\n",
    "fn_corr": 0,
    "fp_corr": 6,
    "fn_miss": 65,
    "fp_over": 110,
    "score": 0.025760334247106893,
    "fitness_history": [
      0.025861818744985016
    ],
    "island_id": 1,
    "easy_overrides": 91
  },
  {
    "id": "iter2_cand0_ad73d9_reset_5648_reset_2099",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    \n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n        # Pattern from analysis: FN cases often have positive spare capacity,\n        # moderate fill ratios, and are not extremely variant in dimensions.\n        # They're \"underestimated\" by the linear model.\n        \n        # Case 1: Moderate fill ratio with sufficient spare capacity and low variance\n        if (0.65 <= fill_ratio <= 0.85 and \n            spare_capacity > 8000 and\n            sku_length_var < 50 and \n            sku_width_var < 10 and \n            sku_height_var < 12):\n            return 1\n            \n        # Case 2: Lower fill ratio with positive spare capacity (conservative SVM)\n        if fill_ratio < 0.7 and spare_capacity > 2000:\n            return 1\n            \n        # Case 3: Items are small on average but not too many\n        if (sku_average_volume < 3000 and \n            sku_counts <= 12 and \n            spare_capacity > 5000):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        # Pattern from analysis: FP cases often have high fill ratios,\n        # high item count, or extreme variations that make packing difficult.\n        \n        # Case 1: Very high fill ratio with many items\n        if fill_ratio > 0.92 and sku_counts > 10:\n            return 0\n            \n        # Case 2: High total area usage with significant item count\n        if wl_to_vehicle_wl_total > 1150 and sku_counts > 9:\n            return 0\n            \n        # Case 3: Large items (high avg dimensions) with moderate-high fill\n        if (sku_length_avg > 24 and sku_width_avg > 10 and \n            fill_ratio > 0.8 and sku_counts >= 8):\n            return 0\n            \n        # Case 4: High minimum WL ratio suggesting large indivisible items\n        if wl_to_vehicle_wl_min > 55 and sku_length_var > 45:\n            return 0\n\n    return -1",
    "fn_corr": 65,
    "fp_corr": 51,
    "fn_miss": 0,
    "fp_over": 65,
    "score": 0.6959570023672063,
    "fitness_history": [
      0.7198270766199328
    ],
    "island_id": 1,
    "easy_overrides": 769
  },
  {
    "id": "iter1_cand0_a24749",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    \n    # Correct FN cases: SVM says NO but it's actually feasible\n    # These often have moderate fill ratios, positive spare capacity, and aren't extremely variant in dimensions\n    if svm_prob < 0.5:  # SVM predicts NO\n        # Conditions that suggest it might actually be YES despite SVM saying NO:\n        # 1. Moderate fill ratio with sufficient spare capacity\n        # 2. Not too much variation in item sizes (easier to pack)\n        # 3. Reasonable aspect ratios\n        \n        # Heuristic: When there's enough spare capacity and items aren't too varied\n        if (0.6 <= fill_ratio <= 0.85 and \n            spare_capacity > 5000 and \n            sku_length_var < 60 and \n            sku_width_var < 12 and \n            sku_height_var < 15):\n            return 1\n            \n        # Special case for lower fill ratios where SVM is underconfident\n        if fill_ratio < 0.7 and spare_capacity > 0:\n            return 1\n    \n    # Correct FP cases: SVM says YES but it's actually infeasible\n    # These often have high fill ratios, many items, or extreme variations\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        # Conditions that suggest it might actually be NO despite SVM saying YES:\n        # 1. Very high fill ratio with many items\n        # 2. Extreme item size variations making packing difficult\n        # 3. High wl_to_vehicle_wl_total indicating potential overcrowding\n        \n        # Heuristic: Too many items for the space available, even if fill_ratio seems okay\n        if fill_ratio > 0.9 and sku_counts > 12:\n            return 0\n            \n        # High total area usage suggests overcrowding\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 10:\n            return 0\n            \n        # High minimum width-length product can indicate large items that don't fit well\n        if wl_to_vehicle_wl_min > 60 and sku_length_var > 50:\n            return 0\n    \n    return -1",
    "fn_corr": 65,
    "fp_corr": 38,
    "fn_miss": 0,
    "fp_over": 78,
    "score": 0.647018878937572,
    "fitness_history": [
      0.6637925943444654
    ],
    "island_id": 2,
    "easy_overrides": 586
  },
  {
    "id": "iter5_cand0_1aa1c4",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    sku_std_length = features.get('sku_std_length', 0)\n    sku_std_width = features.get('sku_std_width', 0)\n    sku_std_height = features.get('sku_std_height', 0)\n    l_to_L_ratio_avg = features.get('l_to_L_ratio_avg', 0)\n    w_to_W_ratio_avg = features.get('w_to_W_ratio_avg', 0)\n    h_to_H_ratio_avg = features.get('h_to_H_ratio_avg', 0)\n    l_to_L_ratio_std = features.get('l_to_L_ratio_std', 0)\n    w_to_W_ratio_std = features.get('w_to_W_ratio_std', 0)\n    h_to_H_ratio_std = features.get('h_to_H_ratio_std', 0)\n    \n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n        \n        # Pattern: Moderate-high fill with good spare capacity and low dimensional variance\n        # This addresses cases where SVM is overly conservative due to high fill but actual packing is possible\n        if (0.75 <= fill_ratio <= 0.85 and \n            spare_capacity > 12000 and\n            sku_length_var < 40 and \n            sku_width_var < 8 and \n            sku_height_var < 10):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity and moderate item count\n        # Addresses cases where low-medium utilization doesn't prevent packing due to ample remaining space\n        if (0.55 <= fill_ratio < 0.75 and \n            spare_capacity > 7000 and\n            sku_counts <= 14):\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        # Handles scenarios where small items can be packed even with moderate counts\n        if (sku_average_volume < 3000 and \n            sku_counts <= 12 and \n            spare_capacity > 8000):\n            return 1\n            \n        # Pattern: Low fill ratio with moderate counts and good spare capacity, even with some variance\n        # Covers cases where despite some size variation, ample space allows for packing\n        if (fill_ratio < 0.65 and \n            sku_counts <= 13 and \n            spare_capacity > 9000 and\n            sku_length_var < 55):\n            return 1\n            \n        # Pattern: Items with relatively uniform dimensions (low std) and moderate fill\n        # These are easier to pack than the SVM might assume\n        if (fill_ratio < 0.8 and\n            sku_std_length < 6 and\n            sku_std_width < 2.5 and\n            sku_std_height < 3):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        \n        # Pattern: Very high fill ratio with high item count\n        # Classic overpacking scenario\n        if fill_ratio > 0.92 and sku_counts > 10:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        # Indicates potential space constraints despite SVM approval\n        if wl_to_vehicle_wl_total > 1250 and sku_counts > 7:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        # Large items become harder to fit as space fills up\n        if (sku_length_avg > 24 and sku_width_avg > 10 and \n            fill_ratio > 0.8 and sku_counts >= 8):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        # Suggests difficulty in fitting irregularly sized large items\n        if wl_to_vehicle_wl_min > 55 and sku_length_var > 45:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        # Indicates presence of very large items that may not fit in remaining space\n        if wl_to_vehicle_wl_max > 185 and fill_ratio > 0.8:\n            return 0\n            \n        # Pattern: High dimensional variance with moderate fill and item count\n        # Variance in multiple dimensions makes packing complex\n        if (sku_length_var > 50 and \n            sku_width_var > 9 and \n            fill_ratio > 0.7 and \n            sku_counts >= 7):\n            return 0\n            \n        # Pattern: High standard deviations in dimensions with high fill ratio\n        # Dimensional inconsistency leads to packing inefficiencies\n        if (sku_std_length > 6.5 and \n            sku_std_width > 2.8 and \n            sku_std_height > 3.2 and \n            fill_ratio > 0.75):\n            return 0\n            \n        # Pattern: High aspect ratio items with moderate-high fill\n        # Long, thin items are notoriously difficult to pack efficiently\n        if max_asr > 6.5 and fill_ratio > 0.75 and sku_counts > 6:\n            return 0\n            \n        # Pattern: Items that are long relative to vehicle length with moderate variance\n        # Difficult to orient and fit in constrained spaces\n        if (l_to_L_ratio_avg > 0.45 and \n            l_to_L_ratio_std > 0.1 and\n            fill_ratio > 0.7):\n            return 0\n\n    return -1",
    "fn_corr": 22,
    "fp_corr": 33,
    "fn_miss": 43,
    "fp_over": 83,
    "score": 0.3068781430954415,
    "fitness_history": [
      0.3114716447121537
    ],
    "island_id": 2,
    "easy_overrides": 342
  },
  {
    "id": "iter9_cand0_325b73",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    sku_std_length = features.get('sku_std_length', 0)\n    sku_std_width = features.get('sku_std_width', 0)\n    sku_std_height = features.get('sku_std_height', 0)\n    l_to_L_ratio_avg = features.get('l_to_L_ratio_avg', 0)\n    w_to_W_ratio_avg = features.get('w_to_W_ratio_avg', 0)\n    h_to_H_ratio_avg = features.get('h_to_H_ratio_avg', 0)\n    l_to_L_ratio_std = features.get('l_to_L_ratio_std', 0)\n    w_to_W_ratio_std = features.get('w_to_W_ratio_std', 0)\n    h_to_H_ratio_std = features.get('h_to_H_ratio_std', 0)\n\n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n\n        # Pattern: Moderate-high fill with good spare capacity and low dimensional variance\n        # This addresses cases where SVM is overly conservative due to high fill but actual packing is possible\n        if (0.68 <= fill_ratio <= 0.85 and \n            spare_capacity > 10000 and\n            sku_length_var < 45 and \n            sku_width_var < 9 and \n            sku_height_var < 11):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity and moderate item count\n        # Addresses cases where low-medium utilization doesn't prevent packing due to ample remaining space\n        if (0.5 <= fill_ratio < 0.72 and \n            spare_capacity > 6000 and\n            sku_counts <= 13):\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        # Handles scenarios where small items can be packed even with moderate counts\n        if (sku_average_volume < 2900 and \n            sku_counts <= 12 and \n            spare_capacity > 7000):\n            return 1\n            \n        # Pattern: Low fill ratio with moderate counts and good spare capacity, even with some variance\n        # Covers cases where despite some size variation, ample space allows for packing\n        if (fill_ratio < 0.65 and \n            sku_counts <= 12 and \n            spare_capacity > 8000 and\n            sku_length_var < 50):\n            return 1\n            \n        # Pattern: Items with relatively uniform dimensions (low std) and moderate fill\n        # These are easier to pack than the SVM might assume\n        if (fill_ratio < 0.75 and\n            sku_std_length < 5.5 and\n            sku_std_width < 2.5 and\n            sku_std_height < 3):\n            return 1\n            \n        # New FN correction pattern: High spare capacity with moderate fill and high item count\n        # SVM may underestimate feasibility when there's ample space even with many items\n        if (fill_ratio <= 0.75 and \n            spare_capacity > 12000 and\n            sku_counts >= 10):\n            return 1\n            \n        # Additional FN case: Very low fill with high item count - SVM might be too conservative\n        if (fill_ratio < 0.55 and \n            sku_counts >= 10 and \n            spare_capacity > 9000):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        \n        # Pattern: Very high fill ratio with high item count\n        # Classic overpacking scenario\n        if fill_ratio > 0.9 and sku_counts > 9:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        # Indicates potential space constraints despite SVM approval\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 6:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        # Large items become harder to fit as space fills up\n        if (sku_length_avg > 23.5 and sku_width_avg > 9.5 and \n            fill_ratio > 0.75 and sku_counts >= 7):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        # Suggests difficulty in fitting irregularly sized large items\n        if wl_to_vehicle_wl_min > 50 and sku_length_var > 40:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        # Indicates presence of very large items that may not fit in remaining space\n        if wl_to_vehicle_wl_max > 180 and fill_ratio > 0.75:\n            return 0\n            \n        # Pattern: High dimensional variance with moderate fill and item count\n        # Variance in multiple dimensions makes packing complex\n        if (sku_length_var > 45 and \n            sku_width_var > 8 and \n            fill_ratio > 0.65 and \n            sku_counts >= 6):\n            return 0\n            \n        # Pattern: High standard deviations in dimensions with high fill ratio\n        # Dimensional inconsistency leads to packing inefficiencies\n        if (sku_std_length > 6 and \n            sku_std_width > 2.5 and \n            sku_std_height > 3 and \n            fill_ratio > 0.7):\n            return 0\n            \n        # Pattern: High aspect ratio items with moderate-high fill\n        # Long, thin items are notoriously difficult to pack efficiently\n        if max_asr > 6 and fill_ratio > 0.7 and sku_counts > 5:\n            return 0\n            \n        # Pattern: Items that are long relative to vehicle length with moderate variance\n        # Difficult to orient and fit in constrained spaces\n        if (l_to_L_ratio_avg > 0.42 and \n            l_to_L_ratio_std > 0.09 and\n            fill_ratio > 0.65):\n            return 0\n            \n        # New FP correction pattern: Very high fill ratio with moderate item count but low spare capacity\n        # Even moderate counts can be problematic at very high utilization levels\n        if (fill_ratio > 0.92 and \n            sku_counts > 6 and \n            spare_capacity < 5000):\n            return 0\n            \n        # Additional FP case: High fill with moderate counts and low spare capacity\n        if (fill_ratio > 0.85 and \n            sku_counts > 8 and \n            spare_capacity < 6000):\n            return 0\n\n    return -1",
    "fn_corr": 25,
    "fp_corr": 50,
    "fn_miss": 40,
    "fp_over": 66,
    "score": 0.3976420078139342,
    "fitness_history": [
      0.40782442887113063
    ],
    "island_id": 2,
    "easy_overrides": 579
  },
  {
    "id": "iter2_cand0_ad73d9",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    \n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n        # Pattern from analysis: FN cases often have positive spare capacity,\n        # moderate fill ratios, and are not extremely variant in dimensions.\n        # They're \"underestimated\" by the linear model.\n        \n        # Case 1: Moderate fill ratio with sufficient spare capacity and low variance\n        if (0.65 <= fill_ratio <= 0.85 and \n            spare_capacity > 8000 and\n            sku_length_var < 50 and \n            sku_width_var < 10 and \n            sku_height_var < 12):\n            return 1\n            \n        # Case 2: Lower fill ratio with positive spare capacity (conservative SVM)\n        if fill_ratio < 0.7 and spare_capacity > 2000:\n            return 1\n            \n        # Case 3: Items are small on average but not too many\n        if (sku_average_volume < 3000 and \n            sku_counts <= 12 and \n            spare_capacity > 5000):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        # Pattern from analysis: FP cases often have high fill ratios,\n        # high item count, or extreme variations that make packing difficult.\n        \n        # Case 1: Very high fill ratio with many items\n        if fill_ratio > 0.92 and sku_counts > 10:\n            return 0\n            \n        # Case 2: High total area usage with significant item count\n        if wl_to_vehicle_wl_total > 1150 and sku_counts > 9:\n            return 0\n            \n        # Case 3: Large items (high avg dimensions) with moderate-high fill\n        if (sku_length_avg > 24 and sku_width_avg > 10 and \n            fill_ratio > 0.8 and sku_counts >= 8):\n            return 0\n            \n        # Case 4: High minimum WL ratio suggesting large indivisible items\n        if wl_to_vehicle_wl_min > 55 and sku_length_var > 45:\n            return 0\n\n    return -1",
    "fn_corr": 65,
    "fp_corr": 51,
    "fn_miss": 0,
    "fp_over": 65,
    "score": 0.6959570023672063,
    "fitness_history": [
      0.7198270766199328
    ],
    "island_id": 3,
    "easy_overrides": 769
  },
  {
    "id": "iter6_cand0_564ce8",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    sku_std_length = features.get('sku_std_length', 0)\n    sku_std_width = features.get('sku_std_width', 0)\n    sku_std_height = features.get('sku_std_height', 0)\n    l_to_L_ratio_avg = features.get('l_to_L_ratio_avg', 0)\n    w_to_W_ratio_avg = features.get('w_to_W_ratio_avg', 0)\n    h_to_H_ratio_avg = features.get('h_to_H_ratio_avg', 0)\n    l_to_L_ratio_std = features.get('l_to_L_ratio_std', 0)\n    w_to_W_ratio_std = features.get('w_to_W_ratio_std', 0)\n    h_to_H_ratio_std = features.get('h_to_H_ratio_std', 0)\n\n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n\n        # Pattern: Moderate-high fill with good spare capacity and low dimensional variance\n        # This addresses cases where SVM is overly conservative due to high fill but actual packing is possible\n        if (0.7 <= fill_ratio <= 0.85 and \n            spare_capacity > 10000 and\n            sku_length_var < 45 and \n            sku_width_var < 9 and \n            sku_height_var < 11):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity and moderate item count\n        # Addresses cases where low-medium utilization doesn't prevent packing due to ample remaining space\n        if (0.5 <= fill_ratio < 0.7 and \n            spare_capacity > 6000 and\n            sku_counts <= 13):\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        # Handles scenarios where small items can be packed even with moderate counts\n        if (sku_average_volume < 2900 and \n            sku_counts <= 11 and \n            spare_capacity > 7000):\n            return 1\n            \n        # Pattern: Low fill ratio with moderate counts and good spare capacity, even with some variance\n        # Covers cases where despite some size variation, ample space allows for packing\n        if (fill_ratio < 0.6 and \n            sku_counts <= 12 and \n            spare_capacity > 8000 and\n            sku_length_var < 50):\n            return 1\n            \n        # Pattern: Items with relatively uniform dimensions (low std) and moderate fill\n        # These are easier to pack than the SVM might assume\n        if (fill_ratio < 0.75 and\n            sku_std_length < 5.5 and\n            sku_std_width < 2.3 and\n            sku_std_height < 2.8):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        \n        # Pattern: Very high fill ratio with high item count\n        # Classic overpacking scenario\n        if fill_ratio > 0.9 and sku_counts > 9:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        # Indicates potential space constraints despite SVM approval\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 6:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        # Large items become harder to fit as space fills up\n        if (sku_length_avg > 23.5 and sku_width_avg > 9.5 and \n            fill_ratio > 0.75 and sku_counts >= 7):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        # Suggests difficulty in fitting irregularly sized large items\n        if wl_to_vehicle_wl_min > 50 and sku_length_var > 40:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        # Indicates presence of very large items that may not fit in remaining space\n        if wl_to_vehicle_wl_max > 180 and fill_ratio > 0.75:\n            return 0\n            \n        # Pattern: High dimensional variance with moderate fill and item count\n        # Variance in multiple dimensions makes packing complex\n        if (sku_length_var > 45 and \n            sku_width_var > 8 and \n            fill_ratio > 0.65 and \n            sku_counts >= 6):\n            return 0\n            \n        # Pattern: High standard deviations in dimensions with high fill ratio\n        # Dimensional inconsistency leads to packing inefficiencies\n        if (sku_std_length > 6 and \n            sku_std_width > 2.5 and \n            sku_std_height > 3 and \n            fill_ratio > 0.7):\n            return 0\n            \n        # Pattern: High aspect ratio items with moderate-high fill\n        # Long, thin items are notoriously difficult to pack efficiently\n        if max_asr > 6 and fill_ratio > 0.7 and sku_counts > 5:\n            return 0\n            \n        # Pattern: Items that are long relative to vehicle length with moderate variance\n        # Difficult to orient and fit in constrained spaces\n        if (l_to_L_ratio_avg > 0.42 and \n            l_to_L_ratio_std > 0.09 and\n            fill_ratio > 0.65):\n            return 0\n\n    return -1",
    "fn_corr": 19,
    "fp_corr": 50,
    "fn_miss": 46,
    "fp_over": 66,
    "score": 0.3527808795657108,
    "fitness_history": [
      0.36167058342744624
    ],
    "island_id": 3,
    "easy_overrides": 570
  },
  {
    "id": "iter10_cand0_325b73",
    "code": "# -*- coding: utf-8 -*-\n'''\n3D-BPP Rule Patch — SVM Hard Case Corrector\n\nThis function is called AFTER SVM prediction to correct hard cases.\nParameters:\n    svm_prob : float  — P(y=1) from SVM (already sigmoid of decision score)\n    features : dict   — raw feature dict with all 41 feature values\n\nReturns:\n    -1  : do not override SVM (rule not triggered)\n    0   : override → predict NO (not feasible)\n    1   : override → predict YES (feasible)\n\nIMPORTANT:\n  - fill_ratio is a dimensionless fraction (often ~0–1): loaded volume / vehicle capacity.\n    It is NOT cubic meters; use the Feature scales table in the prompt for thresholds.\n  - Base your decision on physical 3D packing constraints:\n      * sku_counts: number of items\n      * sku_average_volume: avg item volume\n      * fill_ratio = total_skuvolume / vehicle_capacity (same as fraction of capacity used)\n      * sku_min_length/max_length, sku_min_width/max_width, sku_min_height/max_height\n      * aspect_ratio (max_asr): longest / shortest dimension of any item\n      * spare_capacity: remaining vehicle volume\n      * sku_length_var, sku_width_var, sku_height_var: variance in item sizes\n      * l_to_L_ratio_avg/min/max: SKU length / vehicle length ratio\n      * etc.\n  - Rules should only fire in specific feature ranges, not globally\n  - Do NOT just return 0 or 1 unconditionally\n  - Test your rule mentally on these patterns before writing code:\n      * High fill_ratio + many items → likely infeasible\n      * Low fill_ratio → likely feasible\n      * Very high aspect ratio items → hard to pack\n'''\nimport math\n\ndef apply_rule_patch(svm_prob: float, features: dict) -> int:\n    # Extract key features\n    sku_counts = features.get('sku_counts', 0)\n    fill_ratio = features.get('fill_ratio', 0)\n    max_asr = features.get('max_asr', 1.0)\n    spare_capacity = features.get('spare_capacity', 0)\n    sku_average_volume = features.get('sku_average_volume', 0)\n    wl_to_vehicle_wl_total = features.get('wl_to_vehicle_wl_total', 0)\n    wl_to_vehicle_wl_min = features.get('wl_to_vehicle_wl_min', 0)\n    sku_length_var = features.get('sku_length_var', 0)\n    sku_width_var = features.get('sku_width_var', 0)\n    sku_height_var = features.get('sku_height_var', 0)\n    sku_length_avg = features.get('sku_length_avg', 0)\n    sku_width_avg = features.get('sku_width_avg', 0)\n    sku_height_avg = features.get('sku_height_avg', 0)\n    wl_to_vehicle_wl_max = features.get('wl_to_vehicle_wl_max', 0)\n    sku_min_length = features.get('sku_min_length', 0)\n    sku_max_length = features.get('sku_max_length', 0)\n    sku_min_width = features.get('sku_min_width', 0)\n    sku_max_width = features.get('sku_max_width', 0)\n    sku_min_height = features.get('sku_min_height', 0)\n    sku_max_height = features.get('sku_max_height', 0)\n    sku_std_length = features.get('sku_std_length', 0)\n    sku_std_width = features.get('sku_std_width', 0)\n    sku_std_height = features.get('sku_std_height', 0)\n    l_to_L_ratio_avg = features.get('l_to_L_ratio_avg', 0)\n    w_to_W_ratio_avg = features.get('w_to_W_ratio_avg', 0)\n    h_to_H_ratio_avg = features.get('h_to_H_ratio_avg', 0)\n    l_to_L_ratio_std = features.get('l_to_L_ratio_std', 0)\n    w_to_W_ratio_std = features.get('w_to_W_ratio_std', 0)\n    h_to_H_ratio_std = features.get('h_to_H_ratio_std', 0)\n\n    # Correct FN cases: SVM says NO but it's actually feasible\n    if svm_prob < 0.5:  # SVM predicts NO\n\n        # Pattern: Moderate-high fill with good spare capacity and low dimensional variance\n        # This addresses cases where SVM is overly conservative due to high fill but actual packing is possible\n        if (0.68 <= fill_ratio <= 0.85 and \n            spare_capacity > 10000 and\n            sku_length_var < 45 and \n            sku_width_var < 9 and \n            sku_height_var < 11):\n            return 1\n            \n        # Pattern: Low-medium fill ratio with substantial spare capacity and moderate item count\n        # Addresses cases where low-medium utilization doesn't prevent packing due to ample remaining space\n        if (0.5 <= fill_ratio < 0.72 and \n            spare_capacity > 6000 and\n            sku_counts <= 13):\n            return 1\n            \n        # Pattern: Small average volume with moderate item count and decent spare capacity\n        # Handles scenarios where small items can be packed even with moderate counts\n        if (sku_average_volume < 2900 and \n            sku_counts <= 12 and \n            spare_capacity > 7000):\n            return 1\n            \n        # Pattern: Low fill ratio with moderate counts and good spare capacity, even with some variance\n        # Covers cases where despite some size variation, ample space allows for packing\n        if (fill_ratio < 0.65 and \n            sku_counts <= 12 and \n            spare_capacity > 8000 and\n            sku_length_var < 50):\n            return 1\n            \n        # Pattern: Items with relatively uniform dimensions (low std) and moderate fill\n        # These are easier to pack than the SVM might assume\n        if (fill_ratio < 0.75 and\n            sku_std_length < 5.5 and\n            sku_std_width < 2.5 and\n            sku_std_height < 3):\n            return 1\n            \n        # New FN correction pattern: High spare capacity with moderate fill and high item count\n        # SVM may underestimate feasibility when there's ample space even with many items\n        if (fill_ratio <= 0.75 and \n            spare_capacity > 12000 and\n            sku_counts >= 10):\n            return 1\n            \n        # Additional FN case: Very low fill with high item count - SVM might be too conservative\n        if (fill_ratio < 0.55 and \n            sku_counts >= 10 and \n            spare_capacity > 9000):\n            return 1\n\n    # Correct FP cases: SVM says YES but it's actually infeasible\n    elif svm_prob >= 0.5:  # SVM predicts YES\n        \n        # Pattern: Very high fill ratio with high item count\n        # Classic overpacking scenario\n        if fill_ratio > 0.9 and sku_counts > 9:\n            return 0\n            \n        # Pattern: High total WL usage with significant item count\n        # Indicates potential space constraints despite SVM approval\n        if wl_to_vehicle_wl_total > 1200 and sku_counts > 6:\n            return 0\n            \n        # Pattern: Large items with moderate-high fill and sufficient item count\n        # Large items become harder to fit as space fills up\n        if (sku_length_avg > 23.5 and sku_width_avg > 9.5 and \n            fill_ratio > 0.75 and sku_counts >= 7):\n            return 0\n            \n        # Pattern: High minimum WL ratio indicating large indivisible items with high length variance\n        # Suggests difficulty in fitting irregularly sized large items\n        if wl_to_vehicle_wl_min > 50 and sku_length_var > 40:\n            return 0\n            \n        # Pattern: Very high maximum WL ratio with moderate-high fill\n        # Indicates presence of very large items that may not fit in remaining space\n        if wl_to_vehicle_wl_max > 180 and fill_ratio > 0.75:\n            return 0\n            \n        # Pattern: High dimensional variance with moderate fill and item count\n        # Variance in multiple dimensions makes packing complex\n        if (sku_length_var > 45 and \n            sku_width_var > 8 and \n            fill_ratio > 0.65 and \n            sku_counts >= 6):\n            return 0\n            \n        # Pattern: High standard deviations in dimensions with high fill ratio\n        # Dimensional inconsistency leads to packing inefficiencies\n        if (sku_std_length > 6 and \n            sku_std_width > 2.5 and \n            sku_std_height > 3 and \n            fill_ratio > 0.7):\n            return 0\n            \n        # Pattern: High aspect ratio items with moderate-high fill\n        # Long, thin items are notoriously difficult to pack efficiently\n        if max_asr > 6 and fill_ratio > 0.7 and sku_counts > 5:\n            return 0\n            \n        # Pattern: Items that are long relative to vehicle length with moderate variance\n        # Difficult to orient and fit in constrained spaces\n        if (l_to_L_ratio_avg > 0.42 and \n            l_to_L_ratio_std > 0.09 and\n            fill_ratio > 0.65):\n            return 0\n            \n        # New FP correction pattern: Very high fill ratio with moderate item count but low spare capacity\n        # Even moderate counts can be problematic at very high utilization levels\n        if (fill_ratio > 0.92 and \n            sku_counts > 6 and \n            spare_capacity < 5000):\n            return 0\n            \n        # Additional FP case: High fill with moderate counts and low spare capacity\n        if (fill_ratio > 0.85 and \n            sku_counts > 8 and \n            spare_capacity < 6000):\n            return 0\n\n    return -1",
    "fn_corr": 25,
    "fp_corr": 50,
    "fn_miss": 40,
    "fp_over": 66,
    "score": 0.3976420078139342,
    "fitness_history": [
      0.40782442887113063
    ],
    "island_id": 3,
    "easy_overrides": 579
  }
]